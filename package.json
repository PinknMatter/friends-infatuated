{
  "name": "friends-infatuated",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test:audio": "esbuild scripts/audio-sim.entry.ts --bundle --format=esm --outfile=scripts/audio-sim.bundle.mjs --platform=node --log-level=error && node scripts/audio-sim.mjs"
  },
  "dependencies": {
    "p5": "^1.11.3"
  },
  "devDependencies": {
    "@types/p5": "^1.7.6",
    "playwright-core": "^1.61.1",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
